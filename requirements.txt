fastapi==0.115.2  # Safe recent version
pydantic==2.9.2    # Matches the core below
pydantic-core==2.23.4  # This version has pre-built wheels for Render/Python
uvicorn==0.30.6
requests==2.32.3   # If you use it for APIs
# Add any other deps you have, like pandas if for analysis
